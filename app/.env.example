# Développement local (backend sur http://localhost:5000)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Production Vercel / APK mobile (build statique Capacitor)
# NEXT_PUBLIC_API_URL=https://life-os-platform.onrender.com/api

# Build Android : copier la racine .env.example vers ../.env (APP_NAME, APP_ID)
